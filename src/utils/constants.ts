const COOKIE_KEY = {
    TOKEN: 'api_token'
}

const KEY_INCLUDE_USER = {
    PERMISSION: 'permission',
    ROLE: 'role',
    USER: 'user'
}

const KEY_INCLUDE_CUSTOMER = {
    PERMISSION: 'permission',
    ORDER: 'order',
    USER: 'user'
}

const KEY_INCLUDE_ORDER = {
    DETAIL: 'detail',
    USER: 'user'
}

const CONSTANTS = {
    COOKIE_KEY,
    KEY_INCLUDE_USER,
    KEY_INCLUDE_CUSTOMER,
    KEY_INCLUDE_ORDER
}

export default CONSTANTS